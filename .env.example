# Frontend (Vite)
VITE_BACKEND_URL=   # Leave empty in dev (Vite proxy handles it). Set to backend URL in production.

# Backend (Bun)
PORT=3001
BASE_URL=http://localhost:3001   # Public URL of the backend (used in upload response URLs)
FRONTEND_URL=http://localhost:5173  # Allowed CORS origin
